# MySQL Tips

To **start** MySQL:

```
$> cd /usr/local/mysql
$> sudo ./bin/mysqld_safe
```

To **dump** a database:

`$> /usr/local/mysql/bin/mysqldump button_football --user root > mysql_button_football.sql`
